HELP.md
.gradle
# 모든 모듈의 빌드 산출물(search-service/build, search-indexer/build, event-contract/build 포함)
build/
!gradle/wrapper/gradle-wrapper.jar

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr
# IntelliJ 컴파일 출력 디렉토리. 반드시 루트에 앵커(/out/)한다 — 앵커 없는 out/는 헥사고날 패키지
# (application/port/out, adapter/out 등 src 하위 out 디렉토리)까지 매칭해 신규 소스 파일이 조용히 누락된다.
# (부모 디렉토리가 제외되면 하위 파일은 !부정 규칙으로도 되살릴 수 없으므로 앵커가 유일한 해법이다.)
/out/

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

### VS Code ###
.vscode/

### Kotlin ###
.kotlin

### macOS ###
.DS_Store

### Claude Code (로컬 에이전트 상태 — 커밋하지 않음) ###
.claude/

# k6 부하 테스트 raw 요약 export(일시적 산출물)
load/results/raw/
